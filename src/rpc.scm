(declare (unit rpc))
(use tcp json-rpc-client)
(define-values (i o) (tcp-connect "icfpc2013.cloudapp.net" 80))
(define icfp-server (json-rpc-server i o))
(define (make-train-call auth-code)
  (icfp-server "train" auth: auth-code)
)
